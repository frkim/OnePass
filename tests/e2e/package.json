{
  "name": "onepass-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end Playwright tests for the OnePass application.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.7.0",
    "typescript": "^5.9.3"
  }
}
